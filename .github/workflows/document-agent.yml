name: Document Agent CI

on:
  push:
    paths:
      - 'quickstarts/document-agent/**'
      - '.github/workflows/document-agent.yml'
  pull_request:
    paths:
      - 'quickstarts/document-agent/**'
      - '.github/workflows/document-agent.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12', '3.13']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Run tests
        working-directory: quickstarts/document-agent
        run: python -m unittest discover -s tests -v
      - name: Run demo
        working-directory: quickstarts/document-agent
        run: python app.py
